version: 2

build:
  os: ubuntu-24.04
  tools:
    python: mambaforge-latest

conda:
  environment: ci/requirements/readthedocs.yml

sphinx:
  builder: html
  configuration: docs/conf.py

python:
  install:
    - method: pip
      path: .
